# MPEG-TS(HLS) 변환 핵심 로직 상세 가이드

이 문서는 MP4 컨테이너에서 추출한 데이터를 MPEG-TS 스트림으로 변환하는 `ts.py`의 핵심 원리와, 개발 과정에서 적용된 수식 및 상수의 의미를 명확히 이해하기 위해 작성되었습니다.

---

## 1. Timescale 90,000의 비밀: 왜 90kHz인가?

`ts.py` 코드 곳곳에서 `90000`이라는 숫자를 볼 수 있습니다. 이는 단순히 임의의 숫자가 아니라 MPEG-2 TS 표준에서 정의된 **시스템 시간 클록(System Time Clock, STC)**의 기본 주파수입니다.

### 90,000을 사용하는 세 가지 핵심 이유

#### ① 다양한 프레임 레이트와의 완벽한 정수 배율 (Integer Multiples)
90,000은 방송 및 비디오에서 주로 사용되는 프레임 레이트들과 정수 단위로 딱 떨어지는 성질을 가지고 있습니다.
- **24 fps**: 90,000 / 24 = **3,750**
- **25 fps**: 90,000 / 25 = **3,600**
- **30 fps**: 90,000 / 30 = **3,000**
- **50 fps**: 90,000 / 50 = **1,800**
- **60 fps**: 90,000 / 60 = **1,500**

이렇게 정수로 떨어지기 때문에 소수점 오차 없이 정확한 프레임 위치(PTS)를 계산할 수 있습니다. 만약 1초를 1,000(ms)으로 잡았다면 24fps의 한 프레임은 41.666...ms가 되어 시간이 지날수록 미세한 오차가 누적될 수 있습니다.

#### ② 고정밀 동기화 (Lip-Sync)
비디오 데이터(보통 24~60Hz)와 오디오 데이터(44.1kHz~48kHz)는 서로 다른 샘플링 레이트를 가집니다. 90kHz는 이 두 스트림의 재생 시점을 하나의 공통된 기준(Master Clock) 위에 정교하게 배치할 수 있을 만큼 충분히 높은 해상도를 제공합니다.

#### ③ MPEG-2 표준 규격 준수
HLS는 기본적으로 MPEG-2 Transport Stream 규격을 따릅니다. 이 규격에서는 PCR(Program Clock Reference), PTS(Presentation Time Stamp), DTS(Decoding Time Stamp)를 모두 90kHz 기반으로 관리하도록 정의하고 있습니다.

> [!NOTE]
> **PTS 계산 공식 (ts.py 126-134라인 참고)**
> `PTS = (MP4 샘플 시간) * 90,000 / (MP4 파일의 timescale)`
> 이 공식은 서로 다른 timescale을 가진 MP4 소스를 표준 TS 규격인 90kHz 시간대로 통일하는 역할을 합니다.

---

## 2. NAL Unit 변환 로직 (AVCC to Annex B)

### 2.1 비디오 데이터의 두 가지 얼굴
H.264 비디오는 NAL(Network Abstraction Layer) 유닛의 집합입니다. 하지만 MP4에 저장된 방식과 실시간 스트리밍(TS)에서 쓰이는 방식이 서로 다릅니다.

1.  **AVCC (MP4 내부 방식)**:
    - 구조: `[Size(4바이트)] + [NAL 데이터]`
    - 특징: 각 NAL 유닛의 크기가 앞에 명시되어 있어 파일 탐색이 빠릅니다.
2.  **Annex B (MPEG-TS 방식)**:
    - 구조: `[00 00 00 01 (Start Code)] + [NAL 데이터]`
    - 특징: 스트림 형식으로, 어디서든 데이터를 읽기 시작해도 스타트 코드를 찾으면 프레임의 시작을 알 수 있습니다.

### 2.2 _NAL_Convert 함수의 원리
`ts.py`의 `_NAL_Convert` 함수는 AVCC 방식의 4바이트 크기 정보를 읽어낸 뒤, 이를 `00 00 00 01`로 바꿔주는 작업을 수행합니다. 이 과정에서 크기 정보를 잘못 읽으면 NAL 유닛의 중간이 잘리거나 다른 데이터와 섞여 **Malformed SEI payload** 같은 치명적인 오류가 발생하게 됩니다.

---

## 3. MPEG-TS 패킷화 과정

MPEG-TS는 모든 데이터를 **188바이트**의 고정 크기 패킷으로 나눕니다.

- **PAT (Program Association Table)**: 어떤 프로그램이 있는지 알려주는 지도입니다.
- **PMT (Program Map Table)**: 특정 프로그램의 비디오 PID, 오디오 PID가 무엇인지 알려줍니다.
- **PES (Packetized Elementary Stream)**: 실제 미디어 데이터에 PTS(시간 정보) 헤더를 붙인 형태입니다.

`ts.py`는 `_TS_Packet` 함수를 통해 이 패킷들을 생성하며, 188바이트를 채우기 위해 부족한 공간은 `0xFF`로 채우는(Stuffing) 작업을 수행합니다.

---

## 4. 학습 포인트: 왜 기존에 재생이 안 되었는가?

1.  **잘못된 NAL 파싱**: MP4 샘플에서 NAL 유닛을 추출할 때 정확한 바이트 크기만큼 읽지 못해 스타트 코드가 엉뚱한 위치에 삽입되었습니다.
2.  **코덱 정보 부재**: 키프레임 직전에 SPS/PPS를 넣어주지 않아 플레이어가 비디오의 해상도나 코덱 프로필을 즉시 알 수 없었습니다.
3.  **HLS.js의 엄격함**: 브라우저는 보안과 안정성을 위해 규격에 조금이라도 어긋나는 바이너리 패킷을 만나면 `Fatall Error`를 내며 재생을 중단합니다.

이번 수정을 통해 이 세 가지 문제를 모두 해결하였습니다.
